apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: ClusterIP  # Internal service for backend communication
  selector:
    app: backend
  ports:
  - port: 5000
    targetPort: 5000

