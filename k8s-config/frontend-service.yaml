apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: NodePort  # Exposes the frontend on a Minikube port
  selector:
    app: frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80